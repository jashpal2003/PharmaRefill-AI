'use client';

// Header.tsx is no longer used in the redesigned 3-screen layout.
// Kept as a stub to avoid breaking any lingering references.

import React from 'react';

export const Header: React.FC<{ children?: React.ReactNode }> = ({ children }) => {
  return null;
};
